name: Test Wok

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build the containers
        run: docker compose build
      
      - name: Start the containers
        run: docker compose up
      
      - name: Run Tests
        run: docker exec --name api go test -v